# 仅开发：Vite 把浏览器的 /api/* 代理到此地址（须与后端 uvicorn 监听地址一致）
# 若后端是 python -m uvicorn ... --port 8002，则改为：
# VITE_DEV_API_PROXY=http://127.0.0.1:8002
VITE_DEV_API_PROXY=http://127.0.0.1:8005

# 若设置非空，前端会直接请求该 URL（绕过代理，需后端 CORS）。开发时建议删除本行，使用上面代理。
# VITE_API_BASE=http://127.0.0.1:8005
