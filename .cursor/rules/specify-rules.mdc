# SmartChef 智能对话管理平台 — 项目导航

> **最高指导原则**: 本规则遵循 `.specify/memory/constitution.md` 项目章程。
> 章程是项目的最高约束，所有编码、测试、审查、发布决策 MUST 与章程保持一致。
> 当本规则与章程冲突时，以章程为准。
> 关键章程条款速查:
> - §III **测试驱动开发（不可协商）**: TDD 强制执行，核心模块覆盖率 ≥ 80%
> - §开发工作流 **代码审查**: 所有合并到主分支的代码 MUST 通过 Code Review
> - §开发工作流 **CI/CD 门禁**: 单元测试 100% + 集成测试 100% + 静态分析零告警

## 文档索引(不要复制这些文档, 按需读取)

| 需要了解什么 | 读哪个文件 |
|-------------|-----------|
| 项目定位、用户故事、功能需求、模块映射 | `specs/master/spec.md` |
| PD 交互设计覆盖状态、模块索引 | `specs/master/pd-all/pd-index.md` |
| 某个模块的交互原型 | `specs/master/pd-all/pd-<module>/` |
| 实施计划、技术栈、阶段规划 | `specs/master/plan.md` |
| 架构设计（模块结构、API 契约、数据流） | `specs/master/ad/` (README.md → 索引) |
| 详细设计（数据模型、状态机、算法、错误码） | `specs/master/dd/` (README.md → 索引) |
| 任务目录与执行进度 | `specs/master/tasks/` (README.md → 索引) |
| 缺陷状态概览（轻量入口） | `specs/master/defects/INDEX.md` |

## 技术栈速查

- **后端**: Python 3.11+ / FastAPI + Pydantic v2 + SQLAlchemy async + Alembic
- **前端**: React 18 + Vite + Ant Design 5 + Zustand (JavaScript, 不用 TypeScript)
- **存储**: PostgreSQL 16 + pgvector + Redis 7.x
- **大模型**: ZenMux API (`base_url=https://zenmux.ai/api/v1`, OpenAI 兼容)
- **测试**: pytest + httpx (后端) / Vitest + Playwright (前端)

## 编码规范

- 后端: TDD, async/await, Pydantic v2 验证
- 前端: 函数式组件, Ant Design, 不使用 TypeScript
- 通用: 中文注释/文档

## 质量门禁（MUST 遵守）

> 来源: `.specify/memory/constitution.md` §开发工作流与质量门禁
> 此规则确保 constitution 中的质量要求在每次实现中被强制执行。

### 测试要求
- 每个业务功能（服务层函数/API 端点/前端页面）MUST 有配套测试
- **代码与测试必须同批次交付**，禁止"先写代码、后补测试"
- 运行 `pytest tests/` 时 MUST 0 失败才能标记任务完成
- 核心模块测试覆盖率 MUST ≥ 80%
- 测试分层: 单元测试(mock DB) → 契约测试(mock 服务层) → 集成测试(真实 DB) → E2E(Playwright)

### Code Review 要求
- 每个模块完成后 MUST 召唤 `code-reviewer` 子代理进行审查
- CRITICAL / MAJOR 问题 MUST 修复后才能标记模块完成
- 全部模块完成后 MUST 做一次全量 Code Review

### 交付物要求
- 项目 README.md MUST 包含: 前置条件、安装步骤、启动命令、环境变量说明、测试运行方式、登录信息
- README MUST 让新开发者 5 分钟内可启动项目

## 实施工作流

运行 `/implement` 自动执行任务. 采用**指挥官+子代理工人**模式:
- 指挥官(主对话): 读 tasks/README.md → 按模块调度 → 派发子代理 → 验证 → 标记进度
- 子代理(工人): 每个工人有独立上下文, 只读取所需的模块任务文件和设计文档, 完成具体任务
- 断点续传: 各模块任务文件中的 `[X]` 标记即进度, tasks/README.md 汇总全局, 新对话 `/implement continue` 自动接续

## 缺陷管理（统一命令: `/speckit.defects`）

> 原 `/speckit.fixbug` 已废弃，缺陷发现、修复、回溯、验证统一由 `/speckit.defects` 管理。

- **缺陷目录**: `specs/master/defects/` (D001.md, D002.md, …)
- **INDEX.md**: 轻量状态表，由 `python scripts/scan_defects.py` 生成，含分类路由建议
- **缺陷模板**: `_template.md`，含 `design_ref` 字段用于设计文档追溯

**缺陷全生命周期**:
1. **扫描发现**: `python scripts/scan_defects.py`（R1-R8 规则检查 + 分类路由）
2. **分类路由**: 根据 category 自动推荐路径（代码修复 / 设计补充 / 需求回溯）
3. **修复实施**: 最小化修改 + 同批次编写测试
4. **设计回溯**: 检查 AD/DD 文档是否需要同步更新，填写 `design_ref`
5. **验证闭环**: 全量测试 + 更新缺陷状态 + 建议 Code Review

**处理缺陷任务时 MUST**:
1. **先执行** `python scripts/scan_defects.py` 更新 INDEX.md
2. **读 INDEX.md** 作为入口，获取缺陷列表、状态、分类、**建议路径**
3. **按需读取** 具体 D*.md，不要全量读取所有缺陷文件
4. **修复后 MUST 执行 Phase 5（设计回溯）+ Phase 6（验证闭环）**

## PD-前端 UI 规格制度（防止 PD 功能缺失）

> 来源: PD 功能缺失根因分析报告 (docs/PD_FEATURE_GAP_ROOT_CAUSE_ANALYSIS.md)
> 涉及缺陷: D031-D036（6 个模块共 40+ 项前端功能缺失）

**核心规则**:
1. **DD MUST 包含前端 UI 组件清单**（§9.5）：统计卡、表格列、筛选器、操作按钮、特殊交互组件
2. **Tasks 的 [F-PAGE] 任务 MUST 附带 PD UI Checklist**：逐项列出 PD 中该页面的所有 UI 元素
3. **implement 模块检查点 MUST 执行 PD UI 覆盖率校验**：覆盖率 < 80% 为 MAJOR 阻塞
4. **Placeholder 页面不得通过模块检查点**：必须实现完整功能或从路由表中移除
5. **Plan 中的项目路径 MUST 与 tasks/implement 中的路径一致**
6. **Code Review MUST 包含 PD 覆盖率维度**
7. **浏览器冒烟验证中 PD 符合性为必检维度**：覆盖率不足即视为未通过
8. **「说明」按钮及 Drawer 不纳入 PD 检查**：各页面的「说明」按钮及其弹出的 Drawer 内容属于 AD/DD 逻辑参考文档，不属于产品功能

## 跨阶段治理红线（证据驱动）

> 目标：把 `spec -> PD -> AD -> DD -> plan -> tasks -> implement -> review -> smoke -> defects`
> 从“流程描述”升级为“强门禁链路”。以下条目均为 MUST，任一不满足都不得宣称完成。

### 单一真相源与冲突前置消解

1. 同一业务规则在 `spec`、`PD`、`AD`、`DD`、`plan` 中出现多套口径时，必须在生成 `tasks` 前回到上游文档消解冲突。
2. 下游阶段不得自行挑选一个“更合理”的版本继续实施；若存在冲突，必须标记为 `BLOCKER`。
3. `AD` 引用的 PD 页面、`DD` 引用的 AD 契约、`plan` 引用的源码路径都必须真实存在且可追溯。
4. `plan.md`、`tasks/`、实现说明、缺陷 `fix_ref` 中引用的源码根路径必须一致；出现多套工程路径时，必须在 `plan` 阶段统一。

### PD 部分覆盖必须显式下传

1. `PD README`、页面备注或设计结论中若出现“部分覆盖 / 暂不覆盖 / 待补充 / 待确认”，必须在 `plan.md` 和 `tasks/` 中显式继承。
2. 被标记为“部分覆盖”的能力不得在 `tasks/README.md` 中显示为 100% 完成，也不得在 `implement` 阶段默认为已设计完成。
3. `tasks` 必须为该类能力显式标注 `Deferred`、`Out of Scope`、`Blocked By` 或 `Stub`，不得把未完成事实隐藏在备注文本中。

### 完成定义必须基于证据

1. 任务、模块、全局完成状态都必须由验证证据驱动，而非主观判断。
2. 任何任务正文、备注、检查点中出现 `未实现`、`UI only`、`placeholder`、`TODO`、`待确认`、`mock`、`stub` 等字样时，该任务不得标记完成，除非已进入显式未完成声明区并同步影响进度。
3. `[B-SERVICE]`、`[B-API]`、`[F-PAGE]` 的完成至少要求：配套测试存在且通过、对应设计引用可追溯、真实成功信号已验证、无假成功提示或占位实现。
4. 模块完成必须附带核心业务链路验证、Code Review、回归结果；缺任何一项都不得在 `tasks/README.md` 中汇总为完成。

### 反假成功规范

1. 前端未真正调用后端接口时，禁止显示“成功”“已生成”“已完成”等成功提示。
2. 异步任务返回 `202 Accepted` 时，前端文案只能表示“已提交任务”，并必须提供状态轮询/查询与失败原因回传。
3. 硬编码统计值、硬编码进度分母、mock 结果、仅更新数据库状态但未执行真实业务逻辑，均视为假成功。
4. 桩实现必须显式返回 `501 Not Implemented` 或抛出带任务编号的 `NotImplementedError`，并记录到延迟/桩实现清单；桩实现存在时不得宣称模块完成。
5. 下载、分页、列表、聊天消息等前后端消费形态必须以真实契约为准，不得让前端猜测响应结构。

### 核心业务链路强制定义

1. 每个 UI 模块必须定义 1-3 条“创建 / 执行 / 验证”闭环链路，并在 `plan.md`、`tasks/`、`smoke` 中持续引用。
2. 核心链路测试不得只覆盖第一步；必须验证关键状态转换、最终结果以及失败分支。
3. 没有核心链路定义的模块，不得进入 `implement`；没有核心链路验证证据的模块，不得进入完成态。

### 缺陷回灌治理

1. 每个缺陷除了 `design_ref`，还应记录 `should_have_been_caught_by`，取值范围至少包含：`spec`、`pd`、`ad`、`dd`、`plan`、`tasks`、`tests`、`review`、`smoke`。
2. 修复缺陷时，必须判断问题本应由哪个阶段拦住，并把结论沉淀回对应命令、模板或规则，而不是只修业务代码。
3. 对高频缺陷模式（假成功、分页契约、异步状态机、下载返回形态等），必须优先模板化或命令化，防止同类问题重复出现。
